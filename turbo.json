{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true,
      "filter": ["./apps/*"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "db:generate": {
      "cache": false,
      "filter": ["./packages/database"]
    },
    "db:migrate": {
      "cache": false,
      "filter": ["./packages/database"]
    }
  }
}